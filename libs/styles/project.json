{
  "name": "styles",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/styles/src",
  "projectType": "library",
  "tags": ["type:ui", "scope:shared", "platform:browser"],
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'No linting for CSS-only library'",
        "cwd": "{workspaceRoot}"
      },
      "cache": true
    }
  }
}
