# services-cases

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build services-cases` to build the library.

## Running unit tests

Run `nx test services-cases` to execute the unit tests via [Vitest](https://vitest.dev/).
