{
  "name": "domain-shared-util",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/domain/shared/util/src",
  "projectType": "library",
  "tags": [
    "type:service",
    "scope:server",
    "platform:node"
  ],
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "libs/domain/shared/util"
      },
      "cache": true,
      "inputs": [
        "default",
        "^default",
        "{workspaceRoot}/libs/services-shared/vitest.config.ts"
      ]
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.json",
        "cwd": "libs/domain/shared/util"
      },
      "cache": true,
      "inputs": [
        "default",
        "^default"
      ]
    }
  }
}
