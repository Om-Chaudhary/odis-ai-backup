/**
 * Legal Layout
 *
 * Layout for legal/compliance pages (privacy policy, terms of service).
 * Uses a minimal layout without marketing navigation.
 */
export default function LegalLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
