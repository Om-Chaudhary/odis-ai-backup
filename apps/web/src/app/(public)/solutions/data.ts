// Solutions page data — all copy lives here, templates rarely change.
// Each entry targets a specific SEO keyword from keyword research.
// Data split into modules in data/ directory per CLAUDE.md file size requirements.

export type { SolutionPageData } from "./data/index";
export { solutions, solutionSlugs } from "./data/index";
