// Comparison page data — all copy lives here, templates rarely change.
// Each entry targets a competitor-related SEO keyword (blue ocean, zero competition).
// Data split into modules in data/ directory per CLAUDE.md file size requirements.

export type { ComparisonRow, DetailedSection, ComparisonPageData } from "./data";
export { comparisons, comparisonSlugs } from "./data";
